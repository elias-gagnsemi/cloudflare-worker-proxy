{
  "name": "cloudflare-worker-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker that acts as a proxy for OpenAI API requests",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.0.0"
  }
}

